# Forum on PHP and MySQL

PHP Warning: Не забудь подправить варнинги в консоле

Todo List:

1. Fix Bugs
      - Отображение постов(начинается с самого первого поста)/комментариев при условии что у пользователя не опубликован пост
        сломанный sql(OFFSET unvalid)
      - Удаления вложенности(Полное удаление вложенностей)
      Вероятное решение: Сначало удаление вложенности после удаление самого поста/комментария
2. Стили и UI(!) +-
    - Стили должны быть адаптивными для любых устройств
    - Починить авторизацию и не только(не полный html block)
    - Добавить возможность просмотреть как будет выглядить пост/комментарий
3. Сделать систему для отправки багов(Баг-репортер)! а так-же отображение статуса обращения
4. Допилить и доработать UserEditor 
    - Добавить возможность добавления не ограниченной возможности смены аватарки для пользователя(на данный момент если пользователь заменит кастомную аватарку на стандартную он более не сможет добавить аватарку) !баг

# Getting started with docker

```bash
    docker build -t petos-forum-app . #in working directory
    
    docker run -p 8081:8081 -it petos-forum-app #start docker image
   ```

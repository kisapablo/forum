{#        <div class="flex-align-center"> #}


{% for post in posts %}
    <div class="post">
        <div class="post-body {# card-body #} bg-light" {# style="border: 1px solid #dddddd;" #}>
           <div class="post-title">
            <a href="{{ url('posts/' ~ post.id) }}">
                <h5 class="post-title">{{ post.title|escape }}</h5></a></div>
            <div class="post-content">Kisapablo Restless <br>
                This forum is for help, discussion, and announcements related to the Rust programming language. Please
                read our code of conduct before participating. We will remove any posts that are not respectful,
                constructive, and o… {# {{ post.content }} #}
            <a href="{{ url('posts/' ~ post.id) }}">Read more</a></div>
            <ul>
            <li class="post-publication-date">{{ post.publication_data|date("F d, Y, H:m ") }}</li></ul>

        </div>
    </div>
    {#        </div> #}
    {#    </div> #}
    {#    </div> #}
{% endfor %}
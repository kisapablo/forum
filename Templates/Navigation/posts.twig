{% for post in posts %}

    <div class="post-size card-body bg-light" {#style="border: 1px solid #dddddd;"#}>
{#        <div class="p-3">#}
{#            <div class="mt-3">#}
        <div class="flex-align-center">
                <div></div>
                <a href="{{ url('posts/' ~ post.id) }}"><h5 class="post-title post-distance">{{ post.title|escape }}</h5></a>
{#        <p class="post-distance">{{ post.content|escape }}</p>#}
                <span class="text-muted post-distance">Kisapablo Restress | {{ post.publication_data|date("F d, Y,") }}</span> <span class="public">{{ post.publication_data|date("H:m") }} Kisapablo Restress</span>
        </div>
        </div>
{#    </div>#}
{#    </div>#}
{% endfor %}
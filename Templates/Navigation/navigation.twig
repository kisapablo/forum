<nav class="navbar navbar-light bg-light fixed-top border-top border-bottom">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ base_url() }}">MyApp</a>
        <div class="d-flex align-items-center">
        {% if userInfo is null %}
            {% if not isAuthenticated %}
            <div class="d-flex">
                <a class="btn btn-outline-primary mx-1" href="{{ url('user/login') }}">Log In</a>
                <a class="btn btn-outline-primary mx-1" href="{{ url('user/registration') }}">Register</a>
                <a type="button" class="btn btn-outline-warning mx-1" href="{{ url('posts/builder') }}">Public new post(Beta) </a>
                <a class="btn btn-outline-info mx-1" href="{{ url('user') }}">Personal Cabinet</a>
                {% else %}
                    <img src="{{ userInfo.avatar }}" alt="Avatar" class="rounded-circle me-2" style="width: 30px; height: 30px;">
                    <span>{{ userInfo.name }}</span>
                    <a class="btn btn-outline-primary mx-1" href="{{ url('user/logout') }}">Log Out</a>
            </div>
        {% endif %}
            {% endif %}
        {% if userInfo is not null %}
            <div>
                {{ userInfo['name'] }}
            </div>
        {% endif %}
    </div>
    </div>
</nav>
{% extends 'base.twig' %}

{% block body %}
    <div {#style="height: 1500px; padding-top: 70px;" #}>
        <div class="container">
            <h3 class="mb-3 mt-3 row row mb-2">Latest Posts</h3>
            <div class="row">
                <div class="posts">
                    {% include 'Navigation/posts.twig' with posts %}
                    <div class="section__">
                        <a href="{{ url('posts') }}">View all</a>
                    </div>
                </div>
                <div class="col-md-3">

{#                    {% include 'Navigation/AuthorAbout.twig' %} #}{# Author Info #}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
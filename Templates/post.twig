{% extends 'base.twig' %}

{% block body %}
    <div class="container">
        <img src="https://dummyimage.com/300x200/05b76a/fff" class="img-fluid" alt="...">
 <h1 class="mt-3 mb-3">Hi!</h1> {# {{ post.title|escape }} #}
        <div class="container mb-3">
            <div class="row">
                <span class="text-muted">Kisapablo Restress | {{ post.published_date|date("F d, Y") }}</span>
            </div>
        </div>
         I'm bug description fix me!{# {{ post.content|raw }} #}
    </div>
{% endblock %}
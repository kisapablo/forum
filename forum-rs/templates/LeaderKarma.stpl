<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaderboard - Forum</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">PetOS Forum</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if let Some(ref u) = self.user { %>
                        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                        <li class="nav-item"><a class="nav-link" href="/posts/builders">New Post</a></li>
                        <li class="nav-item"><a class="nav-link active" href="/posts/leaders">Leaderboard</a></li>
                        <li class="nav-item"><a class="nav-link" href="/user"><%= &u.name %></a></li>
                        <li class="nav-item"><a class="nav-link" href="/user/logout">Logout</a></li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link" href="/user/login">Login</a></li>
                        <li class="nav-item"><a class="nav-link" href="/user/registration">Register</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h2 class="mb-4">Top Karma Leaders</h2>
        <div class="card">
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Rank</th>
                            <th>User</th>
                            <th>Role</th>
                            <th>Total Posts</th>
                            <th>Registered</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (idx, leader) in self.leaders.iter().enumerate() { %>
                            <tr>
                                <td><%= idx + 1 %></td>
                                <td><a href="/posts?author=<%= leader.user_id %>"><%= &leader.en_name %></a></td>
                                <td><span class="badge bg-primary"><%= &leader.en_name %></span></td>
                                <td><%= leader.total %></td>
                                <td><%= leader.registration_date.format("%B %d, %Y").to_string() %></td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personal Cabinet - Forum</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">PetOS Forum</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if let Some(ref u) = self.user { %>
                        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                        <li class="nav-item"><a class="nav-link" href="/posts/builders">New Post</a></li>
                        <li class="nav-item"><a class="nav-link" href="/posts/leaders">Leaderboard</a></li>
                        <li class="nav-item"><a class="nav-link active" href="/user"><%= &u.name %></a></li>
                        <li class="nav-item"><a class="nav-link" href="/user/logout">Logout</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <% if let Some(ref cabinet) = self.cabinet { %>
            <div class="row">
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-body text-center">
                            <% if let Some(ref icons) = self.icons { %>
                                <img src="<%= &icons.icon_name %>" alt="Avatar" class="rounded-circle mb-3" style="width: 150px; height: 150px;">
                            <% } else { %>
                                <img src="/public/images/default_ico/default-avatar.png" alt="Avatar" class="rounded-circle mb-3" style="width: 150px; height: 150px;">
                            <% } %>
                            <% if let Some(ref u) = self.user { %>
                                <h4><%= &u.name %></h4>
                            <% } %>
                            <p class="text-muted"><%= &cabinet.en_name %></p>
                            <% if !cabinet.moto.is_empty() { %>
                                <p class="fst-italic">"<%= &cabinet.moto %>"</p>
                            <% } %>
                        </div>
                    </div>
                </div>
                <div class="col-md-8">
                    <div class="card">
                        <div class="card-header">
                            <h5>Account Information</h5>
                        </div>
                        <div class="card-body">
                            <table class="table">
                                <tr>
                                    <th>Posts</th>
                                    <td><%= cabinet.total %></td>
                                </tr>
                                <tr>
                                    <th>Registered</th>
                                    <td><%= cabinet.registration_date.format("%B %d, %Y").to_string() %></td>
                                </tr>
                                <tr>
                                    <th>Last Active</th>
                                    <td>
                                        <% if let Some(last_visit) = cabinet.last_visit_date { %>
                                            <%= last_visit.format("%B %d, %Y at %H:%M").to_string() %>
                                        <% } else { %>
                                            Never
                                        <% } %>
                                    </td>
                                </tr>
                            </table>
                            
                            <div class="mt-3">
                                <h6>Actions</h6>
                                <% if let Some(ref u) = self.user { %>
                                    <a href="/posts?author=<%= u.id %>" class="btn btn-primary me-2">My Posts</a>
                                    <a href="/user/UserEditor" class="btn btn-warning me-2">Edit Profile</a>
                                    <a href="/user/icons/default" class="btn btn-info me-2">Change Avatar</a>
                                    <% if cabinet.en_name == "Admin" || cabinet.en_name == "helper" { %>
                                        <a href="/admin" class="btn btn-danger">Admin Panel</a>
                                    <% } %>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="alert alert-warning">User profile not found.</div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

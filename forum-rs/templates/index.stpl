<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forum - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">PetOS Forum</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if let Some(ref u) = self.user { %>
                        <li class="nav-item"><a class="nav-link" href="/posts/builders">New Post</a></li>
                        <li class="nav-item"><a class="nav-link" href="/posts/leaders">Leaderboard</a></li>
                        <li class="nav-item"><a class="nav-link" href="/user">
                            <img src="<%= &u.icon_name %>" alt="Avatar" style="width: 25px; height: 25px; border-radius: 50%;" class="me-1">
                            <%= &u.name %>
                        </a></li>
                        <li class="nav-item"><a class="nav-link" href="/user/logout">Logout</a></li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link" href="/user/login">Login</a></li>
                        <li class="nav-item"><a class="nav-link" href="/user/registration">Register</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h2 class="mb-4">Latest Posts</h2>
        
        <!-- Search and Filter -->
        <div class="card mb-4">
            <div class="card-body">
                <form method="get" action="/posts" class="row g-3">
                    <div class="col-md-4">
                        <input type="search" class="form-control" name="search" placeholder="Search posts...">
                    </div>
                    <div class="col-md-3">
                        <input type="search" class="form-control" name="author" placeholder="Author ID">
                    </div>
                    <div class="col-md-3">
                        <select name="tags" class="form-select">
                            <option selected>All Tags</option>
                        </select>
                    </div>
                    <div class="col-md-2">
                        <button type="submit" class="btn btn-primary w-100">Search</button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Posts -->
        <% for post in &self.posts { %>
            <div class="card mb-3">
                <div class="card-body">
                    <h5 class="card-title">
                        <a href="/posts/<%= post.post.id %>" class="text-decoration-none"><%= &post.post.title %></a>
                    </h5>
                    <p class="card-text"><%= &post.post.content %></p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <img src="<%= post.post.author_icon_name.as_ref().unwrap_or(&"/public/images/default_ico/default-avatar.png".to_string()) %>" 
                                 alt="Avatar" style="width: 30px; height: 30px; border-radius: 50%;" class="me-2">
                            <small class="text-muted">
                                <%= &post.post.author_name %> • 
                                <%= post.post.publication_date.format("%B %d, %Y").to_string() %>
                            </small>
                        </div>
                        <div>
                            <% for tag in &post.tags { %>
                                <span class="badge bg-secondary me-1"><%= &tag.name %></span>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        <% } %>

        <!-- Pagination -->
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                <% if self.pagination.current > 1 { %>
                    <li class="page-item">
                        <a class="page-link" href="?page=<%= self.pagination.current - 1 %>">Previous</a>
                    </li>
                <% } %>
                <% let start_page = if self.pagination.current > 2 { self.pagination.current - 1 } else { 1 }; %>
                <% let end_page = std::cmp::min(self.pagination.pages_count, start_page + 4); %>
                <% for page in start_page..=end_page { %>
                    <li class="page-item <% if self.pagination.current == page { %>active<% } %>">
                        <a class="page-link" href="?page=<%= page %>"><%= page %></a>
                    </li>
                <% } %>
                <% if self.pagination.current < self.pagination.pages_count { %>
                    <li class="page-item">
                        <a class="page-link" href="?page=<%= self.pagination.current + 1 %>">Next</a>
                    </li>
                <% } %>
            </ul>
        </nav>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
